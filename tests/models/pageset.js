const { PageSet } = require('../../src/models/pageset');

describe('getPages', () => {
  it('works', () => {
    expect('TODO').toEqual('');
  });
});

describe('getParams', () => {
  it('works', () => {
    expect('TODO').toEqual('');
  });
});

describe('getGlobalConfig', () => {
  it('works', () => {
    expect('TODO').toEqual('');
  });
});

describe('getPageNameToConfig', () => {
  it('works', () => {
    expect('TODO').toEqual('');
  });
});
